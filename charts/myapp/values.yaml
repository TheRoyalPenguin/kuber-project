replicaCount: 3
image:
  repository: penguinchik/sre-practice-api
  pullPolicy: Always
  tag: "v1"

service:
  type: ClusterIP
  port: 80

# Настройки для зависимого чарта Redis
redis:
  architecture: standalone
  auth:
    enabled: false # Для простоты
